# Admin OpenAPI bundle

The bundled entry file lives next to the main spec: **`../../src/docs/openapi/admin-index.yaml`** (same `paths/` and `components/` tree as the main API).

When the server runs, **Swagger UI** for this bundle is served at **`/admin-docs`**. The public catalog remains at **`/docs`**.
