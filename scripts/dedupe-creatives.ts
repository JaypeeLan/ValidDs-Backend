/**
 * @deprecated Use the lightweight runner (no ts-node OOM):
 *
 *   npm run dedupe-creatives -- --market US --dry-run
 */
console.error('Use: npm run dedupe-creatives -- --market US [--dry-run]');
process.exit(1);
