# Admin documentation

- **Markdown:** **[`endpoints.md`](./endpoints.md)** — HTTP reference for **`/api/v1/admin/*`** (JWT + admin role).
- **OpenAPI (Swagger UI):** **`/admin-docs`** when the API server is running (bundled from [`../src/docs/openapi/admin-index.yaml`](../src/docs/openapi/admin-index.yaml)).

Public routes: Swagger UI at **`/docs`** (catalog, products, profile/region, auth, billing, Shopify, user TikTok live).

**Admin & ops** (this folder + **`/admin-docs`**): `/admin/*`, `/jobs/*`, `/ingestion/trigger`, TikTok operator routes.
